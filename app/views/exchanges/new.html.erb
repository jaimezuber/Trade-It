<% # simple_form_for @exchange, remote: true do |f| %>
<%= simple_form_for @exchange do |f| %>
  <%= f.input :name %>
  <%= f.input :read_key %>
  <%= f.input :write_key %>
  <div class="d-flex justify-content-center">
    <%= f.submit "Save Exchange", class: 'btn btn-primary' %>
  </div>
<% end %>
